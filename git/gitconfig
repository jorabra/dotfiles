[user]
	name = Jørgen Abrahamsen
	email = jorgen.abrahamsen@gmail.com

[branch "master"]
	remote = origin
	merge = refs/heads/master

[color]
    ui = auto
    #branch = auto
    #diff = auto
    #status = auto
[color "branch"]
    current = yellow reverse
    local = yellow 
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan

[alias]
    ss = stash
    sp = stash pop
    sl = stash list

    ca = commit --amend --reuse-message=HEAD

    l = log --oneline --graph --all --decorate
